# Overlay SFX

Sounds are generated with the Web Audio API in the OBS browser source
(`useOverlaySfx`) — no binary assets required.

Optional: drop short `.mp3` / `.wav` files here later if you switch to file-based playback.
